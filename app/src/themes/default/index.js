// Default theme for colors
import { default as colors } from './colors'

// Default theme for fonts styling
import { default as fonts } from './fonts'

// Default spacing for elements
import { default as spacing } from './spacing'

export default {
  colors,
  fonts,
  spacing,
}

